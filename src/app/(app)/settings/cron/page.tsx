import { ComingSoon } from "@/components/coming-soon";

export default function SettingsCronPage() {
  return (
    <ComingSoon
      title="Cron setup"
      description="Copy /api/tick URL + token for cron-job.org — PRD §12."
    />
  );
}
