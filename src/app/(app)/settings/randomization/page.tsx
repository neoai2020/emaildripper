import { ComingSoon } from "@/components/coming-soon";

export default function SettingsRandomizationPage() {
  return (
    <ComingSoon
      title="Randomization tuning"
      description="Sliders + live histogram for feels_human pacing — PRD §9, §11."
    />
  );
}
