import { ComingSoon } from "@/components/coming-soon";

export default function SettingsOverviewPage() {
  return (
    <ComingSoon
      title="Settings"
      description="Global preferences, alerts, cron URL, backups — entry point for sub-pages in PRD §24."
    />
  );
}
