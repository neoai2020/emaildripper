import { ComingSoon } from "@/components/coming-soon";

export default function SettingsPreferencesPage() {
  return (
    <ComingSoon title="Preferences" description="Tooltips toggle, theme (dark v1), default timezone — PRD §22." />
  );
}
