# Supabase (create project at https://supabase.com — link repo / DO later)
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=

# Worker cron (cron-job.org → GET /api/tick?token=...)
CRON_TOKEN=

# HMAC for Make.com outbound payloads (shared with scenarios)
MAKE_HMAC_SECRET=
